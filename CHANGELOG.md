<!-- 版本更新日志：每个版本改了什么，按时间倒序补充在文件末尾 -->

# Abbey Road — 大学生课表与日程管理

本地优先的课表应用：Android APK + Windows EXE，双端**共用同一套界面代码**（`app/` 目录），
数据只存在本机，通过手动导入/导出 JSON 在两端同步。

规划文档见上一版交付：`Abbey-Road-产品与架构规划-v0.2.0.md`（本工程实现其中的 MVP 部分）。

---

## 目录结构

```
abbey-road/
├─ app/                  ← 唯一的界面实现（HTML/CSS/JS），两个平台共用
│  ├─ index.html
│  ├─ css/               tokens.css（设计变量） app.css（布局与组件）
│  └─ js/                store / schedule / mdparse / location / prompt / bridge / app
├─ android/              ← Android 外壳（Java + WebView + 原生桥）
│  ├─ AndroidManifest.xml
│  ├─ res/               字符串、颜色、主题、图标
│  ├─ src/…/MainActivity.java
│  ├─ tools/ApkBuilder.java   把 dex 与 assets 打进 aapt2 产出的 base.apk
│  └─ build.ps1          一条命令出签名 APK
├─ windows/              ← Windows 外壳（C# + WebView2）
│  ├─ src/Program.cs · MainForm.cs · Bridge.cs · Native.cs
│  ├─ installer/Installer.cs   自研安装器（安装/卸载 + 快捷方式）
│  └─ build.ps1          一条命令出 EXE + 安装器
└─ dist/                 ← 构建产物
```

## 技术路径说明（为什么不是 Flutter）

规划文档原本推荐 Flutter。本机实际情况：

| 依赖 | 状态 |
| --- | --- |
| JDK 17 + Android SDK 34 + build-tools | ✅ 已具备（`$HOME\.android-build`） |
| Visual Studio（含 C++ 桌面工作负载） | ❌ 未安装 → **Flutter Windows 桌面无法构建** |
| Flutter SDK | ❌ 未安装 |
| WebView2 Runtime | ✅ 已安装（152.0.4191.66） |
| .NET Framework 4.8 + Roslyn 编译器 | ✅ 已具备 |

因此 v0.1.0 采用 **「一套 Web 内核 + 两端原生外壳」**：

- 界面与业务逻辑 100% 共用（`app/`），双端观感天然一致 → 满足"双端高度一致"这条硬约束；
- Android：WebView 承载 + Java 原生桥（震动、日历、闹钟、地图、文件读写）；
- Windows：WebView2 承载 + C# 原生桥（窗口亚克力、系统音、文件对话框、导航）；
- 数据模型、JSON 格式、Markdown 导入规范、验收标准**与规划文档完全一致**，
  将来若要迁移到 Flutter，只需重写外壳，`app/` 里的规则与用例可直接复用。

## 构建

```powershell
# Android APK（签名，可直接安装）
cd android; ./build.ps1

# Windows EXE + 安装器 + 绿色版
cd windows; ./build.ps1

# 逻辑回归测试（直接跑发货用的同一份 JS）
node tools/test-logic.js
```

产物输出到 `dist/`。

## v0.1.0 已实现 & 验证状态

已实现（MVP 主体，功能列表对应规划文档 F 系列）：

- 今日页三栏（本周概览 / 今日时间线 / 最近的课）+ 点击右侧进入**聚焦态**，全程 CSS 过渡 ≤300ms；
- 位置 / 时间 / 老师 / 备注四个**毛玻璃弹窗**（遮罩关闭、可编辑保存）；
- 布局体系：`左中右` / `左右`（左列上矮下高）/ `上中下` 三套预设，每栏宽高可用**滑块实时调整**，
  也能直接拖分隔条；尺寸按屏幕断点分别记忆；
- 课程展示：单双周 / 周次范围 / 调课 / 停课 / 补课 / 换教室 / 换时间 / **连堂课自动合并**；
- 导入：JSON 导出/导入（含合并预览与冲突清单）、**MD-1 固定 Markdown 解析**（E001/E010/E020/E030、
  W100–W130、C200/C220 全套提示）+ 逐条修正 + 导入后弹出「学期与节次设置」；
- 设置 8 个分类：外观（主题/主色/毛玻璃强度/动画速度/震动开关与强度）、布局与尺寸、
  课程与课表（学期、节次表）、提醒与通知、导入与同步、系统集成（大学/校区/城市/导航偏好/裁剪规则）、
  数据与备份、关于与帮助；
- 导航：地点自动裁剪（去教室号/楼层）与补全大学名、线上课程不导航、可改查询词后跳转
  （Android：高德/百度/系统地图意图阶梯；Windows：浏览器地图）；
- 系统集成：Android 闹钟与日历走 `ACTION_SET_ALARM` / `ACTION_INSERT` 意图阶梯（失败逐级降级）；
  Windows 导出 `.ics` 并调用系统日历；
- 首次启动 6 步引导（两端一致，可跳过、可重看）；
- Android 12 以下自动降级毛玻璃（功能不变）。

验证情况：

| 项 | 结果 |
| --- | --- |
| 逻辑回归测试（同一份发货 JS） | **66 项全部通过**（Markdown 解析、周次/节次、地点裁剪 10 例、连堂识别、冲突、JSON 合并） |
| Android 真机（vivo / Android 12 / WebView 101） | **已安装运行**：引导、今日页、聚焦态、周表、导入页、设置页、位置弹窗、演示课表全部验证通过 |
| Android 打包 | 签名 APK，`minSdk 26 / targetSdk 34`，权限仅震动 + 闹钟 + 通知 |
| Windows 打包 | 编译通过，产出 `AbbeyRoad.exe`(105 KB) + 安装器 + 绿色版 |
| Windows 运行 | 窗口创建、界面资源解压、WebView2 环境创建均通过；**当前沙箱会话无法启动 WebView2 浏览器进程**（无交互桌面，Edge 也无法运行），需在你的桌面上双击 EXE 才能看到界面 |

## 开发辅助脚本

| 脚本 | 用途 |
| --- | --- |
| `tools/test-logic.js` | 逻辑回归测试（Node，无需浏览器/设备） |
| `tools/serve.js` | 把 `app/` 起成本地静态站点，便于用浏览器调试 |
| `tools/cdp.js` | 通过 DevTools 协议直接读写运行中的应用（Android：`adb forward tcp:9222 localabstract:webview_devtools_remote_<pid>`） |

## v0.1.1 更新（根据使用反馈）

1. **提示词大幅简化，解析器变成"什么格式都能吃"**
   - 提示词从 60 行压缩到 14 行、单张表 6 列（`ar-schedule-md-2.0.0`），不再要求四个区块；
   - 解析器（`md-2.0`）按「JSON 数组 → 标准表格 → 宽松逐行」三级兜底，
     表头缺失、制表符/逗号/空格分隔、Markdown 列表、甚至一行中文散文（`高数 张三 周一1-2节 1-16周 信息楼305`）
     都能还原成课程行；缺周次默认按「全周」处理并给 W140 提示；
   - 调课/停课也支持宽松写法（`高等数学 2026-10-08 调课 周四 1-2 信息楼201`）。
2. **点击动效重做**：按下位置扩散的柔光（ink）、克制的按压缩放 + 抬升阴影 + 更快的按下/更慢的回弹，
   按钮 / 卡片 / 分段控件 / 导航 / 周次条全部统一；键盘焦点环补齐。
3. **设置页左侧分类导航**：8 个分类可点击跳转、滚动时自动高亮；窄屏自动变成顶部横滑条。
4. **周表页直接看整周**：顶部周次快切条（1…总周数，当前周高亮 + "本周"标记）、
   日期条可跳转到某天；手机竖屏按「周一…周日」分组列出本周全部课程（没课的天也显示"没课"）。
5. **今日页三个模块都能点开**：
   - 本周概览展开 → 「本周课表 / 月视图」切换，月视图有课程圆点、点日期即可跳转、可切月份、可回到今天；
   - 今日日程展开 → 每节课列出老师/地点/周次/备注，并带位置·时间·老师·备注四个可点模块；
   - 最近的课展开 → 原聚焦态（下节课详情）；
   - 展开的那一栏放大细化，另外两栏同时缩小简略；✕ 或 Esc 收起；分隔条仍可拖动调整。

验证：逻辑测试 **96 项全部通过**（新增格式容忍、简版提示词、宽松变动等用例）；
Android 真机（vivo / Android 12）实测引导、三栏展开、月视图、周表整周列表、设置导航均正常。

## v0.1.2 更新（第二轮反馈）

1. **设置页恢复滚动**：v0.1.1 在窄屏把内容区设成 `overflow: visible`，导致手机上进不了滚动。
   现在导航与内容各自独立滚动（内容区可滚动高度 3606px），滚动监听会自动高亮当前分类。
2. **今日页"彻底放大"**：点任意一栏，它占满大半个页面（实测 73%–77% 高度），另外两栏压到最小；
   展开态还多了工具行（复制今日安排 / 第一节加入日历；最近的课：打开导航 / 加入日历 / 设闹钟 / 复制课程信息），
   顶栏新增**实时时间**（`17:37 9月13日 · 周日`，每 20 秒刷新），缩放过渡沿用 300ms emphasized 曲线。
3. **导入改用自定义文本格式 AR-TXT v1**：不再依赖 Markdown 表格。
   - 一行一门课，字段用全角竖线 `｜` 分隔（复制成半角 `|` 也认）：
     `课程名｜星期｜节次｜周次｜地点｜老师｜备注`
   - 支持元信息行（`开学日期：`、`总周数：`、`学期：`）与变动行（`调课｜…`、`停课｜…`）；
   - 解析按**位置优先 + 内容校验**，漏写某个字段会自动补位（不会串行）；
   - 导入页新增选项：**学期名称 / 开学第一周周一 / 总周数 / 文本没写单双周时按（全周·单周·双周）**；
   - 旧的 Markdown 表格、制表符、JSON 仍然兼容（有表头时按列名映射）。
4. **设置页导航固定在左侧**：窄屏也不再变成顶部横条，改为 104px 窄侧栏（宽屏 168px），
   可滚动、点击跳转并高亮当前分类。
5. **周表改成课表网格**：一周 7 天的"周X + 日期"在上，节次与上课时间在左列，
   课程块按节次跨行、显示课程名/地点/老师；横向不足时可滑动；周次快切条保留。
6. **新应用图标**：蓝色圆角底 + 白色课表卡片 + 彩色课程格，一眼看出是课表类应用
   （Android 五套密度 + Windows ICO 同时重新生成，生成脚本 `tools/make_app_icons.ps1`）。
7. **在本地 Android 模拟器（MuMu 12）上逐项验收**：安装、启动、三栏放大、月视图、
   周表网格、导入解析与选项、设置导航与滚动、桌面图标全部实测通过。

## v0.1.3 更新（第三轮反馈 + 动效规格书重构）

1. **导入页可以上下滑动**：根因是 `.main` 是 block、`.view` 用 `height:100%`，百分比高度链断裂，
   内容把页面撑高后没有滚动容器。现在整条链改为 flex（app → main → view → pane-scroll），
   实测内容 2296px、可视 1238px、滚动正常。
2. **今日页**：
   - 直板机默认布局改为 **「左右」**（上：本周概览，下：今日日程，右侧：最近的课）；
   - 直板机若选到「左中右」会**自动降级为左右**，避免三栏挤压错位；
   - 三个栏位点击后**一律放大到过半页面**（实测：本周概览 65%×88%、今日日程 65%×90%、最近的课 62%×100%）；
   - 顶栏显示实时时间（每 20 秒刷新），缩放过渡动画保留（340ms CUBIC，规格书里唯一允许的布局动画）。
3. **动效按《动效与光效实现规格书》重构**（毛玻璃材质不变）：
   - 令牌统一到 `--ease-cubic / --ease-smooth / --ease-fade-out / --ease-dialog-in/out` 与 `--dur-*` 语义时长；
   - 按钮点击 = **整颗均匀高光**（92% 白 + 8% 主题色，峰值 0.30，500ms 衰减，裁剪在圆角内），
     去掉了原来的圆形涟漪与外发光；按压 90ms 缩到 0.99、260ms 回弹；
   - 区块入场 rise：60ms 错峰、头部 -24px、其余 +12px、500ms CUBIC；
   - 列表子项只做透明度淡入：30ms 错峰、180ms FADE_OUT；
   - 分段控件：胶囊 420ms SMOOTH 滑动 + 标签 200ms 变色，滑块本身不做高光；
   - 弹窗：入场 300ms（scale 0.92 → 1）/ 出场 200ms（scale → 0.96），遮罩压暗 18%；
   - 卡片去掉了投影，改用描边定义边界（保持毛玻璃不变）；
   - 震动只保留在主要操作（导航/滑块等仅保留视觉反馈）。
4. **设置页导航**：宽度 168→144px、字号 13→12px（窄屏 92px / 10.5px），文字不截断；
   选中项放大到 1.06，切换时平滑缩回（260ms FADE_OUT）。

## v0.1.4 更新（第四轮反馈：真机排版 / 导入清空 / 系统跳转 / 动画卡顿）

1. **真机排版错乱的根因修复**：之前用 JS 计算绝对坐标（left/top/width/height）定位三个栏目，
   在真实手机上会被系统字号、显示缩放、刘海安全区打乱，导致栏目重叠、内容被裁。
   现在：
   - 布局改成 **CSS Grid**（`grid-template-areas` + 尺寸变量），浏览器负责排布，**结构上不可能重叠**；
   - 加 `html { -webkit-text-size-adjust: 100% }`，Android 侧再加 `setTextZoom(100) + setMinimumFontSize(1)`，
     系统字号放大不再把网页文字撑大；
   - 用 `env(safe-area-inset-*)` 处理刘海与手势条。
   - 实测（模拟器系统字号 1.3×）：`overlap=false`、水平方向 `fit=true`。
2. **导入成功后自动清空输入区**：Markdown 文本框与 JSON 粘贴框在应用成功后都会清空，并重置解析结果。
3. **修好地图 / 系统日历 / 闹钟跳转（关键）**：Android 11+ 起，未在清单里声明 `<queries>` 时
   `resolveActivity()` 一律返回 null —— 原来的实现拿它做前置判断，导致整条降级链全部失效。
   现在：
   - 清单补上 `geo` / 日历 `INSERT` / 闹钟 `SET_ALARM` 等 intent 与常见日历时钟包名；
   - 改为**直接 startActivity + try/catch 逐级降级**（不再依赖 resolveActivity）；
   - 日历阶梯：ACTION_INSERT → INSERT_OR_EDIT → 跳到当天 → 拉起 `com.oplus.calendar` /
     `com.coloros.calendar` / `com.android.calendar` → 复制事件信息；
   - 闹钟阶梯：SET_ALARM → SHOW_ALARMS → 拉起 `com.oplus.alarmclock` / `com.coloros.alarmclock` /
     `com.android.deskclock` → 复制时间；
   - 导航阶梯：高德/百度 → `geo:` → 网页地图。
4. **展开动画不再卡顿**：展开/收起不再动 width/height/left/top（那会触发整页重排），
   改为"模板瞬时切换 + 内容 opacity/translate/scale 浮入淡出"（规格书规则 1），
   实测展开后栏目占比：本周概览 91%、今日日程 91%、最近的课 79%。

## 数据与隐私

- 所有数据保存在设备本机的应用存储里（Android：WebView 本地存储；Windows：`%LOCALAPPDATA%\Abbey Road`）。
- 无网络请求、无账号、无云同步；导出 JSON 由用户自己保管与传输。

## v0.1.8 更新（第六轮反馈）

1. **导入页 Tab 改版**：第一行只有两步流程「① 提示词」「② 课表文本」（加粗 + 序号徽标），
   第二行放「手动添加」「配置文件」（不带序号）；原「JSON」在界面上统一改称**配置文件**。
2. **引导重写**：六个步骤重写为"是什么 → 怎么点 → 填什么 → 选布局 → 装课表 → 常去哪"，
   去掉「跳过」按钮（**长按「下一步」约 0.6s 可直接跳过，界面上不做任何提示**），
   底部操作条固定，**「下一步」永远停在卡片右下角**，长内容只在卡片内部滚动。
3. **课表文本页**：顶部加一行"继续下滑"的提示；「应用到课表」改成钉在**右下角的悬浮按钮**，
   粘贴并解析通过后直接点它，不用再滚到底。
4. **设置·外观修复（关键）**：
   - 深色模式下换主题色没反应：原因是 `body[data-theme="dark"]` 里的 `--accent` 盖住了写在
     `:root` 上的行内变量 → 现在主题变量写在 `<body>` 上，深浅色都生效，柔光斑也跟着主题色走；
   - 动画速度没反应：规格书里的时长令牌都是 `calc(... * var(--motion-scale))`，之前只改了旧的
     `--dur-*` → 现在 `--motion-scale = 1 / 速度`，0.5× / 1× / 1.5× 真正生效；
   - 毛玻璃强度：四档同时改**模糊半径**与**面板透明度**（`--glass-k`），
     实测深色下面板 alpha 0.87 / 0.675 / 0.58 / 0.45，档位差别一眼可见；
   - 删除「减少动效」选项（旧数据里的开关在迁移时清掉）。
5. **今日页删掉小白条**：不再长按栏间空白调尺寸，栏位尺寸统一在「设置 → 布局与尺寸」用滑块调整。
6. **布局只保留两种**：设置里只留「左右」「上中下」，旧的「自动」「左中右」在数据迁移时折算成「左右」，
   直板机上不会再出现三栏挤压。
7. **设置页导航再收窄**：104px → 92px、字号 11 → 10.5px（选中项仍会平滑变大加粗），
   九个分类文字全部完整显示。

## v0.1.9 更新（第七轮反馈）

1. **导入页「手动导入」子菜单**：「手动添加」「配置文件」收成两个小按钮，挂在「手动导入」按钮下面，
   点开有开合动画（opacity + translate + 缩放），选中后按钮上标出当前子页（如「手动导入 · 配置文件」），
   点面板外自动收起。
2. **设置齿轮图标重绘**：换成标准齿轮（8 颗圆角齿 + 环形外圈 + 中心孔），比原来的星形清楚。
3. **今日页聚焦放大改用 FLIP 形态过渡**：先量出三栏旧位置，布局切换后按旧位置反算一个
   `translate + scale`，再用强缓出曲线（`cubic-bezier(.22,1,.36,1)`，460ms，与「动画速度」联动）
   播放回原位。全程只动 transform（合成层），不触发重排；实测连续 90 帧无掉帧（最长帧 11ms）。
4. **周表切换动画补全**：「上一周 / 下一周」内容从对应方向浮入（26px + 缩放 + 淡入，340ms），
   「本周」按方向定位，「周视图 ↔ 月视图」原地淡入，「周次条」30ms 错峰淡入，切换时带触觉反馈。
5. **顶栏时间精确到秒**：`22:14:03`，秒数用弱化字号；每秒刷新一次（页面在后台时跳过，回前台立刻补上）。
6. **「最近的课」按离上课时间切状态色**：默认 15 分钟内红色、30 分钟内黄色、上课中蓝色，
   三档都加粗边框 + 柔光 + 状态标签，其它时间保持课程原色（边框不加粗）；
   阈值（1–60 / 2–180 分钟）、三种颜色（12 色可选）、是否加粗边框都能在「设置 → 课程与课表」里改。
7. **开源准备**：新增 GitHub 版 `README.md`（特性 / 架构 / 构建 / 格式说明），原开发日志移入
   `CHANGELOG.md`，补 `.gitignore`；构建脚本里的私人路径与签名口令改成环境变量 /
   `build.local.ps1`（不入库）读取，源码包里不含任何密钥。

## v0.2.0 更新（第八轮反馈）

1. **今日页条目全部可编辑**：「最近的课」卡片里的位置 / 节次 / 时间 / 老师 / 备注每一项都能点，
   其中节次、老师、备注、地点会直接打开编辑窗口（时间弹窗里也加了「编辑时间 / 节次」入口），
   点完全部字段一次改完还可以用「编辑课程」按钮。
2. **周表点课程块 = 详细编辑**：新增「编辑课程」窗口，能改课程名（同名自动合并并统一颜色）、
   老师（多个用、分隔，可增删）、地点、星期、节次（第几节到第几节）、上课时间、单双周 / 周次范围
   （每周 / 单周 / 双周 / 自定义 1-8,10,12）、备注、颜色。
   - 如果这一天本来就有「调课 / 换教室 / 换时间」记录，窗口顶部会出现「仅这次 / 这门课的所有时段」开关，
     默认只改当天那一节；同一天重复编辑只会合并进同一条变动记录，不会越改越多。
   - 改了时间只影响这门课（block 上写死时间），不会动整张节次表。
3. **周表左栏改成「几点到几点」**：第几节缩成一个小角标，下面直接显示 08:00 / 08:45 这样的时间区间。
4. **弹窗遮罩残留修复**：从「课程详情」再打开子弹窗（如设置颜色）后关闭，之前会留下看不见的全屏遮罩，
   点哪里都没反应；现在弹窗只保留一层栈，关闭后彻底隐藏。
5. **Windows 端资源自动刷新**：同一个版本号重新打包 exe 后，%LOCALAPPDATA%\Abbey Road\web\v<版本>
   里的旧界面会自动重解压，不会再出现"装了新版还是旧界面"。
6. **新增回归测试**：tools/smoke-ui.js（CDP 驱动真机 / 模拟器上的真实界面，43 项断言，
   测试用的临时课程与 localStorage 会在结束时还原），tools/test-logic.js 增加到 170 项。

## v0.2.1 更新（第九轮反馈）

1. **配置文件导入修好了**（这轮的重点）：
   - 「导入配置文件」的选择器直接落在「下载」目录，不用在「最近」里翻自己刚导出的那份。
   - **「用其他应用打开 → Abbey Road」**：注册了 `.json` 的 VIEW / SEND，微信里收到的配置文件
     点开就能直接进导入流程（微信的文件在自己的私有目录，系统文件选择器根本看不到，
     这是手机上最靠谱的导入路径）。冷启动、热启动都验证过。
   - 粘贴配置文本：不再藏在折叠面板里，粘进去**自动解析**出预览；空内容、JSON 写坏、
     其实是课表文本这几种情况都会明确提示（还能一键转去「课表文本」页）。
2. **导出到微信**：配置文件页 / 设置 → 导入与同步 / 数据与备份都加了「导出到微信」。
   - Android：先把文件写进「下载 / Abbey Road」，再拉起系统分享面板，选微信就把文件发出去。
   - Windows：没有发到微信的接口，降级为存到「下载」+ 复制内容（拖进微信或直接粘贴发送）。
3. **Windows 端原生调用丢参数的严重 bug**（这轮挖出来的）：WebView2 传过来的 `args`
   实际是 `ArrayList`，旧代码只认 `object[]` 就直接当成空参数 ——
   结果「导出配置文件」会存成 0 字节文件、复制是空串、地图/日历关键词全丢。
   现在两种形态都收，导出/复制/地图/日历恢复正常。
4. **跨设备合并改用「内容指纹」**：两台设备各自生成过 id，同一门课 id 对不上，
   旧逻辑会「导出再导入 = 两份一模一样的课」。现在按开学日期 / 老师姓名 / 地点原文 /
   课程名 / 课程+星期+节次+周次 匹配同一条记录，导入自己的导出是「新增 0 · 更新 0」。
   墓碑（删除记录）也只统计真的会删掉本机数据的那几条，预览里不会再莫名出现「删除 N」。
5. **默认作息换成新版**（12 节）：
   08:00-08:45 / 08:50-09:35 / 10:00-10:45 / 10:50-11:35 /
   13:30-14:15 / 14:20-15:05 / 15:30-16:15 / 16:20-17:05 /
   18:00-18:45 / 18:50-19:35 / 19:40-20:25 / 20:30-22:00。
   只有「还是上一版默认作息」的数据才会自动换过来，自己改过节次时间的保持原样；
   课程只记节次号，所以换时间不会挪动任何一节课。
6. **回归测试**：逻辑测试 192 项（新增跨设备合并 / 自导入幂等 / 新作息迁移），
   界面冒烟测试 54-55 项（新增：粘贴自动解析、导出到微信、不重复添加、Windows 原生参数、
   新作息表），Windows 与 MuMu 模拟器（Android 12）双端跑通，测试数据自动还原。

## v0.2.2 更新（第十轮反馈）

1. **配置文件导入导出的可用性重做**（先说明上一版为什么更难用：v0.2.1 给文件选择器加了
   `EXTRA_INITIAL_URI` 想让它直接落在「下载」，但个别系统（ColorOS 这类自带文件选择器）
   拿到 MediaStore 的 URI 会直接显示空白，看起来就是「点了没反应」）：
   - 去掉那段初始化目录设置，恢复标准选择器；同时把 `.json` 可能被识别成的
     `application/json` / `application/x-json` / `application/octet-stream` 都列进筛选，
     避免刚导出的文件在选择器里被过滤掉。
   - **新增「从导出目录导入」**：App 自己列出导出过的配置文件（Android 读「下载 / Abbey Road」，
     Windows 读「下载」），点一条就能预览导入 —— **完全不依赖系统文件选择器**，
     选择器再难用也不影响这条路径。
   - 「导出配置文件」在选择器不可用时会自动存到「下载 / Abbey Road」并提示文件名，不再静默失败。
   - 设置 → 导入与同步 里同样有「导出到微信 / 从导出目录导入」。
2. **第 13 节及以后的课不再「没时间」**：导入（课表文本 / 手动添加 / 配置文件合并）时，
   一旦发现课表用到的节次超过现有节次表，就自动按「上一节结束后 10 分钟、每节 45 分钟」
   往下补足到需要的节数，并提示「节次表已自动扩到 N 节，时间可在设置里改」。
   原来这些课在周表里画得出来却查不到时间、今日页显示「时间待定」，现在完全正常。
3. **设置齿轮重画**：换成 Material 标准齿轮路径（外圈 8 齿 + 中心孔），小尺寸下更干净。
4. **复制提示词后自动跳到「2 课表文本」**：并弹出「去 DeepSeek / 豆包粘贴并发送课表截图，
   再把回复粘到这里」的引导，粘贴区还会闪一下指示下一步。
5. **清空所有数据改成按住 3 秒**：弹窗里是一条红色进度按钮，按住不放进度走满才执行，
   中途松手立刻取消并提示「数据没有变动」，单击 / 连点都不会清。
6. **顺手修掉两个数据问题**：
   - 重复导入同一份课表时，课程按名字合并了、时段却还挂着对方的课程 id，会攒下
     「周表画不出来、文件越来越大」的孤儿时段；现在导入会把时段正确挂到本地课程上，
     同一条时段重复导入只更新地点 / 老师 / 备注，不再新增。
   - 加载时自动清理历史遗留的孤儿时段 / 变动（模拟器实机验证：时段 83 → 21、变动 36 → 6，
     周表可见课程和 20 周总课次完全不变）。
7. **回归测试**：逻辑测试 205 项（新增：自动扩节次、重复导入幂等、孤儿清理），
   界面冒烟测试 71-72 项（新增：复制提示词跳转、导出目录导入清单、按住 3 秒清空
   的「提前松手不清 / 按满才清」两态），Windows 与 MuMu 模拟器双端跑通，测试数据自动还原。

## v0.2.3 更新（第十一轮反馈：配置文件这块整块重构）

**先说这轮真正修掉的那个 bug**（你反馈的「清空数据后再导入原配置还是空白」）：
清空数据会新建一个空学期；导入的配置带着自己那个学期，合并后课程确实进来了，
但「当前学期」还指着那个空的 —— 界面按当前学期过滤，于是什么都画不出来。
实测复现：清空 + 导入后 `课程 16 门 / 时段 67 条`，而当前学期下 **0 条**，今日页 0 张卡片。

1. **配置文件导入导出整块重构**：全部逻辑收进新模块 `app/js/configio.js`（`AR.ConfigIO`），
   四条出口（另存为 / 导出到微信 / 复制文本 / 系统分享）与三条入口
   （系统选择器 / 导出目录清单 / 粘贴或其它 App 分享过来的文本）都只有一份实现，
   预览、校验、合并也统一走同一个函数，不再散落在界面代码里。
2. **修掉「导入后一片空白」**：合并结束后会自动校正「当前学期」——
   如果当前学期一门课都没有、而数据挂在别的学期上，就切到有课的那个（优先用配置文件里
   标记的当前学期）。导入完成后还会自动跳到「今日」，并把
   「已导入：课程 N 门 · 时段 M 条 · 当前学期「XXX」」明确写出来。
3. **顺带修掉节次表归属问题**：历史数据里出现过「节次挂在已不存在的学期上」，
   平时靠兜底显示，一旦补课次就会只剩新补的那几条（作息全乱）。
   现在补节次前会先把整张表认领到当前学期；加载时也会自动纠正归属。
4. **实测你的完整场景**（MuMu 模拟器 + Windows 双端）：
   导出配置 → 按住 3 秒清空（课程 0）→「从导出目录导入」选那份文件 → 预览「新增 67」→
   合并导入 → 课程 16 门回来、当前学期 17 条时段、周表重新画出 12 个课程块，
   界面自动回到今日页 —— 不再空白。
5. **回归测试**：逻辑测试 214 项（新增：清空后导入要切学期、节次表认领归属），
   界面冒烟测试 74-75 项（新增：清空后导入原配置 → 合并成功 / 当前学期有课 / 周表重新画出），
   Windows 与 Android 双端全绿，测试数据自动还原。

## v0.2.4 更新（第十二轮反馈）

1. **弹窗动画全部统一（重做）**：以前每个弹窗靠 CSS 类切换 + 各自的 transition，
   卡片大小 / 布局（普通卡片 vs 课程编辑器的宽卡片）不一样时观感就不同步 ——
   今日页「时间」弹窗和周表「课程」弹窗进出场明显两种感觉。
   现在所有浮层的进出场都由同一段 JS 驱动：
   - 同一套关键帧：进场 `translateY(14px) + scale(.94) + opacity 0` → 原位；
     离场 `translateY(6px) + scale(.975) + opacity 0`，幅度更小、更快；
   - 同一条非线性曲线：进场 `cubic-bezier(.16,1,.30,1)`（强缓出），
     离场 `cubic-bezier(.40,0,.80,.60)`；
   - 同一时长：进场 300ms / 离场 190ms，跟着「动画速度」设置缩放；
   - 首次引导卡片、遮罩、toast 全部套用同一套（遮罩与卡片同步、不各动各的）；
   - 弹窗打开时不再叠加"脉冲缩放"（那个 scale 会和进场动画抢 transform，
     正是"两种动画感"的来源），只保留震动。
   冒烟测试新增断言：抓「今日页时间弹窗」和「周表课程弹窗」的实际动画参数直接比对，
   要求完全相同，并校验曲线确实是非线性缓动。
2. **今日页聚焦放大：卡顿修掉（重写）**：根因是三栏同时用 CSS transition 动
   `left/top/width/height` —— 每帧都要重排；再加上毛玻璃（backdrop-filter）
   每帧重新模糊。现在：
   - `.zone` 不再过渡 left/top/width/height，只保留 box-shadow/padding；
   - 聚焦全程走 FLIP，只动 transform；
   - 动画期间临时关掉毛玻璃重算（`.morphing` 类），结束立即恢复，玻璃底色不变；
   - 动画期间不再叠加卡片各自的入场动画（少一层合成）。
   Windows 实测：700ms 内 127 帧、平均 5.5ms、**最长帧 5.9ms、0 次掉帧**（展开与收起都一样）。
3. **配置文件改成和「课表文本」一模一样的用法**：配置文件页去掉一堆按钮，
   现在是「复制配置文本 / 导出为文件 / 导出到微信 / 选择 .json / .txt 文件 / 从导出目录导入」
   加一个大的粘贴框，粘贴即自动解析，右下角悬浮「合并导入」——
   和课表文本页的操作方式、位置、动效完全一致（含清空按钮、下滑提示、悬浮按钮切换）。
4. **周表加学期切换**：右上角新增学期按钮（显示当前学期名），点开列出所有学期
   （开学日期 + 周数 + 课程数），点一个就切过去；弹窗里还能直接「新建学期」
   （名称 / 开学第一周周一 / 总周数），建完自动切换。切换后周表、今日页一起更新。
5. **周表点课程卡片也能设日程/闹钟**：课程编辑窗口顶部加了「加入系统日历 / 设置闹钟 /
   打开导航」三个快捷操作，用的是这一次课的日期与时间（周表里点开就能设，不用回今日页）。
6. **「最近的课」倒计时会自动刷新**：以前只有进页面时算一次；现在每 10 秒
   重算一次（从后台回来也会立即补），同节课只改那一行文字，换课/进入 30 分钟、
   15 分钟、上课中等状态色变化时自动整块重画。
7. **课程颜色支持色盘自由选**：课程编辑窗口和「课程颜色」弹窗都加了自定义色盘
   （原生取色器），存 `#RRGGBB`，不限原来的 12 色；周表、今日页都会跟着变。
8. **关于与帮助里贴出开源地址**：新增一张卡片显示
   `https://github.com/EtoileZzz/AbbeyRoad----ScheduleApp`，
   地址本身可点、「在浏览器中打开」按钮交给系统浏览器、「复制地址」一键复制。
9. **回归测试**：界面冒烟测试 104-105 项（新增：弹窗动画一致性、配置文件页新流程、
   学期切换、编辑器快捷操作、色盘、倒计时元素、开源地址跳转），Windows 与 Android 双端全绿。

## v0.2.5 更新（第十三轮反馈）

1. **今日页聚焦放大整套重做（真几何补间）**：上一版用的是 FLIP —— 把整栏
   `transform: scale()` 到旧位置再长回来。实测抓帧发现两个硬伤：
   - 单帧最大形变到了 `scale(2.06, 0.64)`：面板连文字一起被横向拉长、纵向压扁，
     观感诡异，而且每帧都要按新比例重新栅格化（这就是"掉帧更严重"的主因）；
   - 点击的"脉冲反馈"（`scale(1→1.03→1)`，140ms）和 FLIP 抢同一个 transform，
     结果动画头 140ms 被顶掉、之后突然跳一下。
   现在改成：
   - 三栏的矩形由 JS 按设置里的占比算成 **px**，聚焦放大 = 直接用 WAAPI 补间
     `left/top/width/height`。文字始终按最终宽度排版，只会自然重排，**不会被拉伸**；
   - 缓动统一为强缓出 `cubic-bezier(.18,1,.28,1)`（展开 380ms / 收起 330ms，
     跟着「动画速度」设置缩放）：起步快、收尾长，读起来是"自然停下来"；
   - 连续快点不再跳：补间起点改成读"此刻真实画在哪"，上一段没播完也能接着长。
2. **动画中途"变暗"修掉**：旧实现为了省性能，在动画期间用 `.morphing` 把
   毛玻璃的 `backdrop-filter` 关掉 —— 面板会瞬间失去模糊与饱和度，整段发灰发暗。
   现在毛玻璃单独拆成一层 `.zone-glass`：**动画期间它的尺寸保持不变**，
   只有父级裁剪框在变大变小，位置用同一时长/同一条曲线同步补间，
   所以模糊只需要算一次，既不用关模糊也不会掉帧。
3. **内容过渡不再"蒙一层灰"**：内容淡入改成"45% 处就到全不透明"，
   剩下时间只留位移与 1.4% 缩放收尾；旧版把上一栏内容压到 40% 再保持 540ms 的做法删掉了。
4. **点击高光不再糊满整栏**：`.zone-toggle`（✕ / ⤢）缺 `position: relative`，
   它的点击高光覆盖层挂到了最近的定位祖先上 —— 点一下会在整页/整栏白闪 500ms。已修。
5. **直板手机不再"一个字一行"**：是否切 compact 摘要以前只看布局预设，
   现在按实际像素判断（完整内容需要约 250px）。手机上「本周概览」一展开，
   右侧「最近的课」会收成一行摘要，而不是被挤成 120px 的窄条；
   同时窄屏下 compact 栏位隐藏徽标、标题字号收小。
6. **研发工具链**（新增在 `tools/`，都是开源可复用的开发脚本）：
   `morph-shots.js`（把动画定格在任意时刻逐张截图 + 打印三栏真实矩形）、
   `screencast.js`（用 CDP 录"真实合成出来的每一帧"并统计帧间隔）、
   `probe-jumps.js`（连续快点找单帧跳变）、`bench.js` + `trace-run.js`（帧节奏与耗时归因）、
   `dev-push.ps1`（不重编 EXE 直接热更新界面，开发用）、`bump-version.js`（一处改版本号）。
7. **实测**（都用新增的调试脚本量过，不是"看着还行"）：
   - Windows 端用 CDP 录"真实合成帧"：380ms 的展开动画产出 38 帧（10–13ms 一帧），
     定格抓图确认面板几何连续变化（327 → 212 → 193 → 190px），全程无形变、无变暗；
   - 连续快点（每 170ms 一次，共 4 次）：三栏单帧最大位移 57px，没有"跳变"；
   - MuMu 模拟器（Android 12+ WebView）：rAF 间隔稳定 16.67ms，矩形逐帧连续；
   - 逻辑测试 214 项、界面冒烟测试 Windows 105 项 / Android 104 项全部通过。

## v0.2.6 更新（第十四轮反馈）

1. **聚焦放大时"框在长、字在跳"根治（栏框与内容分层）**：上一版把矩形补间做对了，
   但内容仍然跟着栏框逐帧重排 —— 面板从 267px 长到 404px 的过程中，
   卡片宽度、文字折行每一帧都在变，看起来就是"卡顿不流畅"。
   现在每栏多了一层 `.zone-inner`：
   - 展开的那一栏，动画开始前先把内容层按**最终尺寸**钉住（`position:absolute` + 固定 px），
     内容一次性排好版，整段动画里**一帧都不重排**；
   - 栏框照旧做非线性补间，变大时只是把已经排好的内容"露出来"；
   - 动画结束/被打断/切页面时统一 `clearMorph()` 解冻并精确落位，不会残留错位。
   实测（MuMu / Android 12+ WebView）：展开瞬间 inner=374×1191 已就位，
   而栏框只有 267px 宽，`nextBody.scrollWidth === 374` —— 内容确实没再重排。
2. **卡片内部排版重做（回到 v0.2.3 的卡片语言）**：
   - **「最近的课」焦点卡**：时间胶囊 + 「今天/明天/周几」+ 大标题 + 节次与倒计时，
     下面是状态标签；状态色仍然是**只描边 + 一层淡光晕**，不动课程原色。
   - **信息格**：位置 / 节次 / 时间 / 老师 / 周次 / 备注 改成响应式网格
     （`repeat(auto-fit, minmax(146px, 1fr))`）——栏宽时自动两列、窄屏一列。
     旧版是一列到底，一个宽屏行里只有一行小字，又空又散。
     位置与备注是长文本，固定占满一行，其余成对排，网格不会留洞。
   - **长条卡片（今日日程 / 本周列表）**：时间用主题色等宽数字，地点、老师各自成块，
     靠间距分开 —— 以前用 ` · ` 拼成一句话，换行时会留下"行首孤零零一个 ·"。
   - **断行规则**：`overflow-wrap: break-word`（不再用 `anywhere`），
     中文照常换行，但 "北区2号教学楼105（阶梯-白板2）" 不会再被从中间劈开。
   - 展开的「最近的课」删掉了重复三遍的"课程信息"块（周次/地点/备注在信息格里已有），
     连堂这种结构信息单独一行小字说明。
3. **开发环境说明**：这一轮的 Windows 端构建照常产出（EXE + 安装器 + 绿色版），
   但当前这台机器上的自动化会话运行在 Session 0（没有交互桌面），WebView2 无法创建控制器，
   所以界面观感与动效是在 Android 模拟器（同一套 Chromium 内核 + 同一份 `app/` 资源）上实测的。
   为此新增 `tools/headless.ps1`：在无桌面会话里用无头 Edge 起同一套界面（CDP 9333）验证；
   该环境若也不允许无头 Edge，脚本会把退出码与 stderr 打出来方便定位。
4. **回归测试**：界面冒烟测试更新为新的卡片结构（焦点卡 + 6 个信息格 + 两列网格），
   Android 端 **106 项全部通过**；逻辑测试 214 项不变。

## v0.2.7 更新（第十五轮反馈）

1. **多种屏幕 / PPI 下的卡片排布（真机尺寸实测）**：用 adb 把模拟器临时改成
   360×640、411×900、600×800、800×800 等逻辑分辨率逐个截图核对，发现并修掉：
   - **窄屏右栏被挤成 150–180px**：`rectsFor` 给「最近的课」加了 196px 像素下限，
     屏幕够宽时仍然完全按设置里的滑杆走；
   - **半栏只有 80–110px 却排两列**：节次/时间/老师/周次改成按栏宽自适应 ——
     内容 <300px 一列铺满，300–700px 两列，≥700px 四列一排，任何宽度都不会
     "一行只放一个格子、旁边空两格"；
   - **栏内宽度档位**：JS 在每次布局后按**每一栏自己的内容宽度**打
     `w-xs / w-s / w-m / w-l / w-xl`（Chrome 101 没有容器查询，只能这样），
     窄栏收字号与内边距、宽栏放大标题、放大信息格；
   - **极窄栏**（折叠屏外屏、聚焦态里 100px 左右那一栏）：标题不换行、
     摘要只留两行、去掉提示语，不再"一个字一行"；
   - **断行**：`（阶梯-白板2）` 这类括号内容前插零宽空格，优先整块换行，
     不会被从中间劈开；栏间距按视口宽度 10/12/14px 自调（JS 与 CSS 同源）。
2. **开发者模式（长按设置里的「外观」800ms 进入）**：新增 `app/js/motion.js`
   作为**统一的过渡动画注册表**，7 个场景共 47 种样式可选、改完立即生效：
   - 今日页 · 聚焦放大（几何放大 / 回弹 / iOS 顺滑 / 淡出淡入 / 错峰推进 / 快速吸附 / 无动画）
   - 今日页 · 聚焦缩小、卡片内容 · 浮入
   - 弹窗 · 进入（弹起 / 底部推入 / 顶部落下 / 右侧滑入 / 从零放大 / 翻转 /
     底部抽屉 / 模糊聚焦 / 轻微旋入 / 弹性 / 无动画）
   - 弹窗 · 退出、页面切换 · 入场、列表 · 逐条入场
   每个场景都有「试放」，底部有「全部恢复默认」；不进入开发者模式时全部走默认样式，
   普通用户的观感与之前完全一致。长按用 pointerdown + 位移取消实现，不会误伤滚动。
3. **回归测试**：新增 4 项开发者模式用例（长按进入 / 7 个场景齐全 / 换样式即生效且落盘 /
   恢复默认），Android 端冒烟 **110 项全部通过**；另用脚本扫了 47 个样式的时长、
   曲线与关键帧，确认没有 NaN / 无效曲线。

## v0.2.8 更新（第十六轮反馈）

1. **过渡动画追加"大胆"的一组（7 个场景 → 72 种样式）**：上一版有些样式只是换了曲线，
   观感差别小；这次加了真正改变姿态的效果（都在内容层做，纯 transform/opacity/filter）：
   - 今日页聚焦放大：★磁贴翻转（微软 Live Tile 那种绕 Y 轴翻入）、★磁贴拼合
     （卡片一块块拼上，带轻微旋转）、★折纸展开（从上边缘翻开）、★景深推移（放大+模糊推进）、
     ★点击处扩散（以你手指点的位置为原点放大）、★聚光显影、★瀑布逐条；
   - 聚焦缩小：★磁贴翻回、★向内收拢、★折拢收起；
   - 弹窗进入/退出：★磁贴翻转、★内容逐条拼合、★自上而下展开、★落体回弹、★轻微旋入放大、
     ★翻出 / ★向上折起 / ★下落 / ★旋转缩出；
   - 页面入场 / 列表入场：★磁贴翻入、★逐块落下、★景深推近、★逐条翻转。
   全部 72 个样式都用脚本扫过时长、曲线与关键帧，无 NaN / 无效值。
2. **直板手机设置页右侧边框显示不全**：面板带"略微发光描边"（box-shadow 外发光），
   而滚动容器是 `overflow-x: hidden`，光晕在右侧被裁掉，看起来就像右边框缺一条。
   现在设置正文左右各留 8–10px，边框与光晕完整。
3. **调休 / 借课**：周表新增入口，选「来源日期 → 目标日期」，可整体**搬过去**
   （原日不再上课）或**复制过去**（两边都上），执行前有预览（几节课、哪些课、
   目标日是否已有课）。实现复用"单次改动"覆盖表（目标日 move + 来源日 cancel），
   所以周表、今日页、冲突检测、导出配置都会自然跟着变，不动课程模板。
4. **周表长按空白处快速新增**：长按任意空档 480ms 弹出「快速新增」，
   已预填好那天的星期与节次，可切「新增课程 / 考试讲座」两种模式；
   滑动会取消长按，不会误触。写入走和"粘贴导入 / 手动添加"完全相同的解析链路
   （自动合并连堂、必要时自动补节次）。
5. **课程配色方案（按文理分科 / 水课分色系）**：新增 `app/js/palette.js`，
   5 套方案（经典彩色 / 文理分科 / 学术冷调 / 暖阳 / 高对比）。
   类型按课程名自动识别（数学物理→理科、文史英语→文科、思政通识→公共、体育→体育），
   「水课」在课程编辑器里手动指定；一键重排时同类型共用色系、靠深浅区分，
   所有颜色都压过亮度，保证课表里的白字读得清。
6. **新色盘（参考绘图软件）**：替换掉系统 `<input type="color">`，改为
   饱和/明度方块 + 色相条 + 十六进制输入 + 最近使用 + 推荐色；
   拖动、点选、手填都可以。它是独立浮层（不再是"顶掉编辑器"），
   选完颜色回到编辑器继续改别的字段。
7. **回归测试**：新增 15 项用例（配色识别与重排、调休入口与预览、长按热区、
   色盘结构与回调、选色不丢编辑器），Android 端冒烟 **125 项全部通过**。

## v0.2.9 更新（第十七轮反馈）

1. **磁贴翻转重做：以"你点的位置"为轴翻进来**。之前只是围绕面板上边缘做 3D 翻转，
   和你点的卡片没关系。现在：
   - 记录点按坐标，**轴放在点击位置所在的那条边**（点右半屏 → 以右边缘为轴、从右往左翻；
     点左半屏反之），并加大透视（perspective 760px）与起手缩放，读起来就是
     "从我点的那块翻开、放大成这一栏"；
   - 键盘/脚本触发没有坐标时，按这一栏在屏幕的左右位置自动选轴。
2. **进和出合并成同一个选项**：开发者模式从 7 行变成 5 行 ——
   「今日页 · 聚焦放大/缩小」「卡片内容 · 浮入」「弹窗 · 进入/退出」
   「页面切换 · 入场」「列表 · 逐条入场」。每个样式的 in / out 参数一起定义，
   不会再出现"放大用一套、缩小用另一套"的割裂感。旧存档键会自动迁移。
3. **默认动画换成更连贯的一套**：今日页 = **点击处扩散 + 回弹放大**（融合样式）、
   弹窗 = **内容逐条拼合**、页面切换 = **错峰上浮**；进入/退出共用一条曲线。
4. **新增 / 优化样式**（共 50 种，全部去掉了名字前的星号）：
   - 新增：共享轴推进（内容沿 Z 轴推近 / 推远）、逐条推近、模糊聚焦、旋入放大、
     底部抽屉、侧向滑入滑出、落体回弹…
   - 优化：磁贴拼合、折纸展开、景深推移、聚光显影、瀑布逐条都补上了对应的"退出方向"，
     曲线与时长也重新配过（进入略慢、退出更快，符合"进慢出快"的观感习惯）。
5. **周表排版：优先"一屏放下整周"**。行高不再写死 58px，而是按容器可用高度 ÷ 节数算
   （40–78px，窄栏 34–64px），插入 DOM 后再按真实溢出量二次校准；
   行高不够时按优先级取舍（先保课名和节次，其次地点/老师，最后上下课时间），
   字号也跟着行高线性缩放。实测本机一周 9 节全部一屏显示（887 / 891px，无纵向滚动）。
6. **课表颜色可读性再优化**：课块改为"按亮度自动选白字或深色字"
   （白字对比度 <3.0 就切深色 —— 琥珀、暖橙、苔绿这类偏亮颜色立刻清楚很多），
   同时给色块加了一层很淡的上亮下暗渐变和 1px 亮边，同屏深浅不同的课更好分辨。
   实测：数据结构/英语/党史等保持白字，心理健康教育（琥珀）、近代史（青碧）、
   大学物理B1（苔绿）自动切成深色字。
7. **回归测试**：新增 6 项（5 个场景合并后的结构、默认样式、in/out 成对、周表自适应行高、
   一屏放下、课块对比档位），Android 端冒烟 **131 项全部通过**。

## v0.3.0 更新（第十八轮反馈）

1. **周表：把纵向空间全让给课表**。「调休」和「冲突（带数量）」搬到右上角按钮区，
   原来占两块面板的位置彻底腾出来；「周视图 / 月视图」与周次快切并排到一行，
   表头也收紧了。同时修掉一个隐藏的排版 bug：表格 `align-content` 默认是 stretch，
   会把 auto 的表头行拉到 410px（实测），整张课表被顶下去 —— 改成 `start` 后
   表头只有 46px，行高自动涨到 92px，本机一周 9 节完整铺满一屏（1130/1134px）。
   课块仍保留"节次 + 上下课时间 + 地点 + 老师"，只有当行高真的不够时才按
   老师 → 地点 → 时间 的顺序逐级省略。
2. **磁贴翻转铺到所有场景**：卡片内容、弹窗也都能"从点击处翻入"
   （弹窗以点击那一侧的边缘为轴，用 rotateY + 760px 透视，起手 0.92 缩放，比之前更"重"）。
   外观页新增 **「磁贴风格（全局）」** 开关：打开后今日页 / 卡片内容 / 弹窗 /
   页面切换 / 列表统一换成磁贴翻转，关掉就回到逐场景的自定义选择。
3. **配色方案**：把「学术冷调」换成 **莫兰迪色系**（低饱和灰调：理科灰蓝、文科灰玫紫、
   公共灰绿、体育灰驼、水课纯灰），并且**所有方案（含经典彩色）都套用文理/公共/体育/水课
   分色系的方法** —— 同一类型的课共用色族、靠深浅区分，亮色自动配深色字。
4. **开发者工具（随时可关）**：帧率悬浮窗、点按涟漪、8px 基线网格、布局描边、
   动画慢放 0.25×、一键复制诊断信息，外加「关闭全部开发者工具」。
   每个开关都能独立开关且持久化，关掉立即清除全部残留节点与样式。
5. **「本周概览」切日期的反馈重做**：不再整块重画 + 突兀淡入，改成
   ① 内容按方向滑入（往后一天从右进、往前一天从左进）带轻微缩放 + 回弹曲线；
   ② 被选中那天做一次明显的"落定"动画（放大 1.18 回弹 + 主题色描边）；③ 震动加强到 medium。
6. **回归测试**：Android 端冒烟全绿；本周新增的周表自适应、磁贴全局开关、
   莫兰迪方案、开发者工具都会在开发者模式打开时一并验证。

---

## v0.3.0 修复补丁（备注功能）

1. **备注改完没反应 / 删不掉** —— 三处根因一起修：
   - 取值时 `block.note || course.note` 兜底：导入会把同一行的备注同时写进课程和时段，
     于是在备注窗口里清空只清掉了时段那份，课程那份又把旧文案显示回来。现在时段上只要有
     这个字段就以它为准，课程级备注只在老数据缺字段时兜底。
   - 写入时统一写 `item.block`：补课 / 加课那节课的 `block` 是渲染时临时拼出来的对象、
     根本不在仓库里，写进去当场就丢；那天已有调课 / 换教室 / 换时间记录时，真正生效的
     是记录里的值，写 block 也是白改。现在按「字段来自哪一层」写回正确的记录，
     弹窗副标题会写明这次是「只改 X/X 这一次」还是「这门课的所有时段」。
   - 「仅这一次」保存会被降级成 `type:'edit'`，而 `blockOccurrence` 以前不认 edit 记录 ——
     保存后整条单次记录失效（换教室 / 换时间 / 借课连同备注一起丢）。现在按 id 改写原记录、
     保留 type 与 blockId，并且 `blockOccurrence` 正式认领 edit 记录。
2. **「老师」窗口打不开 / 「+ 添加老师」点了没反应**：`openTeacherModal` 里
   `body.querySelector('#teacherList')` 找的是 body 自己（querySelector 只找后代）恒为 null，
   于是有老师时 `list.appendChild` 直接抛异常、没老师时按钮空点。现在改成外层容器 + 列表两层。
3. **地点 / 老师同样走新的写回逻辑**：补课、调课卡片上的地点与老师信息现在也能保存成功。
4. 验证：Android 模拟器（adb + CDP）实机点按 + 键盘输入走完「今日 → 备注 → 保存」全流程，
   换教室单次记录保存后仍是 `room`、界面同步；逻辑回归 214 项全通过，界面冒烟 125 项通过。

---

## v0.3.1 更新（左手模式 · 布局滑块 · 本周概览）

### 一、本周概览：缩略图只看工作日，滑动留给聚焦态

> 这一条是对本节下方"缩略图"那版的修正：**缩略图不该滑动**，
> 滑动只属于"点开聚焦放大"之后的整张周表。

1. **缩略图（折叠态）固定周一到周五、不可滑动**：5 列平分卡片宽度，
   表头上一行「一…五」、下一行日期，一屏看全工作日，永远不出现横向滚动；
   行高按卡片可用高度算并夹在 9–20px，插进 DOM 后按真实溢出量二次校准，
   卡片零溢出（顺便修掉 `.week-table { min-height: 100% }` 被缩略图继承、
   底部白多出一截导致滚动条的问题）。
2. **周末信息不丢**：缩略图只画工作日，所以当这一周周末有课时，
   底部提示会补一句「周末 N 节」；提示按 选中日期 → 周末节数 → 操作提示
   的优先级拼，放不下就逐段削掉，始终只占一行。
3. **「本周概览」展开态才是可以左右滑动的地方**（仅此一处）：
   每天保留 42px 下限，卡片窄时外层 `.wt-scroll` 横向滑动，时间列
   `position: sticky` 吸左，滑到周日也知道是第几节。
4. **展开态点课程块 = 只读课程详情**：只列 时间 / 周次 / 老师 / 地点 / 备注，
   弹窗里只有「关闭」，不再出现 编辑课程 / 设置颜色 / 编辑备注 / 删除。
5. **「周表」页一个字没改**：点课程块仍然是完整编辑器（星期 / 单双周 / 节次 /
   时间 / 老师 / 地点 / 备注 / 颜色 / 删除），列宽、行高、长按新增、调休等全部照旧。

---

### 二、左手模式 · 布局滑块 · 其它修复

1. **左手模式**（设置 → 布局与尺寸）：打开后整块版图左右镜像 ——
   「最近的课」换到左边，本周概览 + 今日日程 换到右边，常用的大卡片落在左手拇指
   够得到的那一侧。实现放在布局引擎的矩形层（`x' = W - x - w`），所以收起态、
   任意一栏的聚焦放大态都会一起镜像，动画补的还是同一批矩形，不会出现
   「某一态忘了换边」。「上中下」布局没有左右之分，开关会给一句说明。
2. **本周概览的缩略图重做**（原「迷你周表」，最终形态见上面第一节）：
   - **一屏铺满**：列宽从"每列 40px、一屏只看得见两天"改成铺满卡片宽度，
     直板机上一眼看出这一周的疏密（最终定为周一到周五 5 列，见第一节）。
   - **行高不再被撑大**：以前是 `minmax(9px, 1fr)`，卡片有多高格子就被拉多高，
     一节连堂课直接变成一块大色砖。现在行高按卡片可用高度算，夹在 9–20px，
     插进 DOM 后再按真实溢出量二次校准，缩略图 + 底部提示必定待在卡片里
     （不再出现底部被裁掉、右边挂一条滚动条）。
   - **表头收成一行**：「9 / 月」原来折成两行，把表头顶到 31px（缩略图六分之一高度），
     现在 8.5px 单行；日期数字行高也跟着收紧。
   - 底部提示按实际情况给，始终只占一行，绝不把卡片顶出滚动条。
3. **布局与尺寸的滑块重做**：以前是一行三列 grid，手机上标签会折成两三行、
   轨道只剩一百来像素，34 档挤在 100px 里根本捏不准。现在标签 + 数值合并成一行、
   轨道独占整行（长了一倍多）；两端加 − / + 微调按钮（点一下 1%，长按 420ms 后
   每 90ms 连跳）；拇指 24px、轨道 6px，已填充部分用主题色画出来；
   滑块加 `touch-action: none`，横向拖动不会再被设置页的纵向滚动抢走；
   拖动按帧（rAF）提交，避免一帧里塞多次重排。
4. **顺带修掉一个状态引用 bug**：`Store.reset()` / `Store.load()` 以前会整个换掉
   state 对象，而 ui.js / panels.js 缓存了引用 —— 表现就是「清空数据后主题、毛玻璃、
   布局设置没跟着恢复」「改设置界面不动」。现在重置改为原地换内容，并新增
   `Store.reload()`（同样是原地加载），冒烟测试的还原流程也改用它。
5. **测试**：冒烟新增「左手模式对调 + 可还原」「缩略图只看周一到周五且不可滑动」
   「展开态点课程块只出只读详情」等检查，
   并把「周表一屏放下」改成「放不下时也必须已压到最小行高」，避免不同屏幕比例下误报。
   Android 模拟器实测：逻辑回归 214 项全通过；手势验证了滑块拖动与 − / + 微调、
   左手模式开关、展开态横向滑动与只读详情。缩略图在 320 / 360 / 412 / 480 / 640px
   五档宽度下逐一核对：5 个工作日全在、行高 13–20px、卡片零溢出、不出现横向滚动。
