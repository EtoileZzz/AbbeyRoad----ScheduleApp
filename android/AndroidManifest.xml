<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.ganxing.abbeyroad"
    android:versionCode="21"
    android:versionName="0.3.0">

    <!-- 震动反馈：重要操作触发 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 跳转系统时钟设置闹钟（声明即可，不是运行时权限） -->
    <uses-permission android:name="com.android.alarm.permission.SET_ALARM" />
    <!-- Android 13+ 通知权限（提醒功能） -->
    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />

    <uses-sdk
        android:minSdkVersion="26"
        android:targetSdkVersion="34" />

    <!-- Android 11+ 的包可见性：不声明这些，resolveActivity / 隐式跳转会全部失败 -->
    <queries>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="geo" />
        </intent>
        <intent>
            <action android:name="android.intent.action.VIEW" />
            <data android:scheme="https" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSERT" />
            <data android:mimeType="vnd.android.cursor.dir/event" />
        </intent>
        <intent>
            <action android:name="android.intent.action.INSERT_OR_EDIT" />
            <data android:mimeType="vnd.android.cursor.item/event" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SET_ALARM" />
        </intent>
        <intent>
            <action android:name="android.intent.action.SHOW_ALARMS" />
        </intent>
        <package android:name="com.autonavi.minimap" />
        <package android:name="com.baidu.BaiduMap" />
        <package android:name="com.oplus.calendar" />
        <package android:name="com.coloros.calendar" />
        <package android:name="com.android.calendar" />
        <package android:name="com.google.android.calendar" />
        <package android:name="com.oplus.alarmclock" />
        <package android:name="com.coloros.alarmclock" />
        <package android:name="com.android.deskclock" />
        <package android:name="com.google.android.deskclock" />
    </queries>

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:launchMode="singleTop"
            android:configChanges="orientation|screenSize|smallestScreenSize|screenLayout|keyboardHidden|uiMode|density"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!--
                微信里收到配置文件后「用其他应用打开」→ 直接进 Abbey Road 导入。
                微信的文件躺在自己的私有目录里，系统文件选择器（作用域存储）看不到，
                所以必须有这两条：VIEW 打开 .json，SEND 分享文件过来。
            -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:mimeType="application/json" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.SEND" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="application/json" />
            </intent-filter>
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="content" android:mimeType="application/octet-stream" android:pathPattern=".*\\.json" />
            </intent-filter>
        </activity>

        <!-- ── 桌面卡片（5 种样式）─────────────────────────────── -->
        <receiver
            android:name=".WidgetHeadProvider"
            android:exported="true"
            android:label="@string/widget_head_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_head_info" />
        </receiver>

        <receiver
            android:name=".WidgetNextProvider"
            android:exported="true"
            android:label="@string/widget_next_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_next_info" />
        </receiver>

        <receiver
            android:name=".WidgetTodayProvider"
            android:exported="true"
            android:label="@string/widget_today_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_today_info" />
        </receiver>

        <receiver
            android:name=".WidgetWeekProvider"
            android:exported="true"
            android:label="@string/widget_week_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_week_info" />
        </receiver>

        <receiver
            android:name=".WidgetShortcutProvider"
            android:exported="true"
            android:label="@string/widget_shortcuts_name">
            <intent-filter>
                <action android:name="android.appwidget.action.APPWIDGET_UPDATE" />
            </intent-filter>
            <meta-data
                android:name="android.appwidget.provider"
                android:resource="@xml/widget_shortcuts_info" />
        </receiver>
    </application>
</manifest>
